name: Code checks

on:
  pull_request:
  push:
    branches: ["master"]

jobs:

  runic:
    name: "Runic"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: '1.11'
      - uses: julia-actions/cache@v2
      - uses: fredrikekre/runic-action@v1
        with:
          version: "1.4" # Keep version in sync with .pre-commit-config.yaml

  explicit-imports:
    runs-on: ubuntu-latest
    name: "ExplicitImports.jl"
    steps:
      - uses: actions/checkout@v7
      # - uses: julia-actions/setup-julia@v3
      #   with:
      #     version: '1'
      - uses: julia-actions/cache@v2
      - uses: julia-actions/julia-buildpkg@v1
      - name: Install dependencies
        shell: julia {0}
        run: |
          # Add ExplicitImports.jl and extension packages
          using Pkg
          Pkg.add([
            PackageSpec(name = "ExplicitImports", version = "1.6"),
          ])
      - name: ExplicitImports.jl code checks
        shell: julia --project {0}
        run: |
          using SpartanArrays, ExplicitImports
          # Check SpartanArrays
          check_no_implicit_imports(SpartanArrays)
          check_no_stale_explicit_imports(SpartanArrays)
          check_all_qualified_accesses_via_owners(SpartanArrays)
          check_no_self_qualified_accesses(SpartanArrays)
